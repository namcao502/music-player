/**
 * Development environment.
 * Use `ng build` (production) to use environment.prod.ts via file replacement.
 */
export const environment = {
  production: false,
  /** Audius API base URL for search, track, and stream. */
  audiusApiUrl: 'https://api.audius.co'
};
